export interface RegisterUserPayload {
	email: string;
	password: string;
	// acceptTerms: boolean;
	// gdprConsent: boolean;
	// teamSharingConsent: boolean;
	// cookieConsent: boolean;
	// ageConsent: boolean;
	// marketingConsent?: boolean;
	// analyticsConsent?: boolean;
}
